{
  "name": "@openscratch/note-deploy-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "OpenScratch Note Cloudflare deployment configuration",
  "author": "OpenScratch Team",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "wrangler pages deploy dist",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@openscratch/note-deploy-cloudflare": "file:"
  }
}
